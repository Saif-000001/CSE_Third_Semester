#include <stdio.h>
#include <math.h>

int main()
{
    // int a, b;
    // scanf("%d%d", &a, &b);

    // printf("%.2f\n", pow(a, b));

    // printf("%.2f\n", sqrt(a));

    // int c = -25;

    // printf("%d\n", abs(c));

    // printf("%d\n", pow(2, 3)); // 0

    float ans = sin(3.141693 / 2);
    printf("%f\n", ans);
    return 0;
}
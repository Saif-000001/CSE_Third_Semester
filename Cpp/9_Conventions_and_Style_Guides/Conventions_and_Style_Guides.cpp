#include <iostream>

int main()
{
    // This code will insult the user

    int slice;
    std::cout << "You fifty how many pieces of pizza you eat ? : ";
    std::cin >> slice;
    std::cout << "You have " << slice << " slice pizza." << std::endl;
    return 0;
}